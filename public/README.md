# public/

Static assets served as-is at the site root (e.g. `/favicon.ico` →
`public/favicon.ico`). Empty for now — favicons, OG fallback images, and
other brand assets are added once branding exists.
