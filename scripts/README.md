# scripts/

One-off or CI maintenance scripts (e.g. content validation, data sync)
that don't belong in `lib/` because they're run standalone via
`tsx`/`node`, not imported by the app. Empty until one is needed.
